<h1>Latest Place</h1>

<div>
	<% if @place.present? %>

Title: <%= @place.title %>

<br />


Address: <%= @place.address %>
<% else %>
<h2> Enter a Place, because we dont have any </h2>

<% end %>
</div>